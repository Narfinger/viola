# viola-code README
Simple extension to control viola from vscode.
This needs viola running on the http://localhost:8088 port.

Install:
- vsce package
- code --install-extension the resulting package.