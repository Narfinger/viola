use dbus::{BusType, Connection, NameFlag};
use dbus::tree::{DataType, Factory, Interface, MTFn};
use std::thread;

use types::*;


pub fn setup(gui: &MainGuiPtr) -> () {
    //thread::spawn(move || {
    //    build(gui)
    //});
}



